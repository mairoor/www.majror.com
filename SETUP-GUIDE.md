# دليل إعداد التحديث التلقائي لملف index.html

## 📋 المتطلبات

1. حساب GitHub مع repository للموقع
2. حساب Netlify مع الموقع المربوط
3. GitHub Personal Access Token

---

## 🔧 خطوات الإعداد

### الخطوة 1: إنشاء GitHub Personal Access Token

1. اذهب إلى: https://github.com/settings/tokens
2. اضغط على **"Generate new token"** → **"Generate new token (classic)"**
3. املأ المعلومات:
   - **Note**: `Netlify Update HTML`
   - **Expiration**: اختر مدة (مثل 90 يوم أو No expiration)
   - **Scopes**: حدد `repo` (Full control of private repositories)
4. اضغط **"Generate token"**
5. **انسخ التوكن فوراً** (لن تتمكن من رؤيته مرة أخرى!)

---

### الخطوة 2: إضافة متغيرات البيئة في Netlify

1. اذهب إلى [Netlify Dashboard](https://app.netlify.com)
2. اختر موقعك
3. اذهب إلى **Site settings** → **Environment variables**
4. أضف المتغيرات التالية:

#### المتغيرات المطلوبة:

| المتغير | القيمة | الوصف |
|---------|--------|-------|
| `GITHUB_TOKEN` | `ghp_xxxxxxxxxxxx` | GitHub Personal Access Token |
| `GITHUB_OWNER` | `your-username` | اسم المستخدم في GitHub |
| `GITHUB_REPO` | `your-repo-name` | اسم الـ repository |

**مثال (للموقع الحالي):**
```
GITHUB_TOKEN = ghp_abc123def456ghi789
GITHUB_OWNER = mairoor
GITHUB_REPO = www.majror.com
```

**ملاحظة**: الموقع الحالي موجود على: https://github.com/mairoor/www.majror.com

5. اضغط **Save**

---

### الخطوة 3: التأكد من أن الملفات موجودة

تأكد من وجود الملفات التالية:

```
netlify/
  └── functions/
      └── update-html.js

netlify.toml
package.json
```

---

### الخطوة 4: نشر التحديثات

1. ارفع الملفات الجديدة إلى GitHub:
   ```bash
   git add .
   git commit -m "إضافة Netlify Function للتحديث التلقائي"
   git push
   ```

2. Netlify سيقوم بنشر التحديثات تلقائياً

---

## ✅ اختبار النظام

1. افتح `admin.html` في المتصفح
2. أضف سيرة ذاتية تجريبية
3. اضغط على **"تحديث تلقائي على GitHub"**
4. انتظر رسالة النجاح
5. تحقق من GitHub - يجب أن ترى commit جديد في `index.html`

---

## 🔍 استكشاف الأخطاء

### خطأ: "GITHUB_TOKEN غير موجود"
- **الحل**: تأكد من إضافة `GITHUB_TOKEN` في Netlify Environment Variables

### خطأ: "GitHub API Error: 404"
- **الحل**: تحقق من `GITHUB_OWNER` و `GITHUB_REPO` - يجب أن تكون صحيحة

### خطأ: "GitHub API Error: 403"
- **الحل**: تأكد من أن التوكن لديه صلاحية `repo`

### خطأ: "GitHub API Error: 422"
- **الحل**: قد يكون هناك مشكلة في تنسيق الملف - تحقق من console logs

---

## 📝 ملاحظات مهمة

1. **الأمان**: لا تشارك `GITHUB_TOKEN` مع أحد
2. **النسخ الاحتياطي**: احتفظ بنسخة من `index.html` قبل التحديث
3. **التحديثات**: التحديثات على GitHub قد تستغرق دقائق قليلة للظهور على الموقع
4. **الطريقة اليدوية**: يمكنك دائماً استخدام "تصدير يدوي" كبديل

---

## 🎯 كيفية الاستخدام اليومي

1. افتح `admin.html`
2. أضف/عدّل السير الذاتية
3. اضغط **"تحديث تلقائي على GitHub"**
4. انتظر رسالة النجاح
5. الموقع سيتم تحديثه تلقائياً خلال دقائق!

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من console في المتصفح (F12)
2. تحقق من Netlify Function logs
3. تحقق من GitHub repository

---

**تم إنشاء هذا الدليل بواسطة: إبراهيم النعيم بليله**

